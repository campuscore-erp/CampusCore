[phases.setup]
nixPkgs = ["libGL", "glib", "libjpeg", "libpng", "zlib", "openssl"]

[phases.install]
cmds = [
  "pip install --upgrade pip setuptools wheel",
  "pip install dlib-bin==19.24.6",
  "pip install face_recognition==1.3.0 --no-deps",
  "pip install 'face-recognition-models>=0.3.0' 'Click>=6.0' 'Pillow' 'numpy'",
  "pip install -r root/requirements.txt"
]

[start]
cmd = "cd root && gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"