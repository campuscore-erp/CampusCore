flask>=3.0.0
flask-jwt-extended>=4.6.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
bcrypt>=4.1.0
Werkzeug>=3.0.0
gunicorn>=21.0.0
pymysql>=1.1.0
cryptography>=41.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
# FIX: dlib-bin==19.24.6 provides a pre-built wheel for dlib.
# Do NOT use "dlib>=19.24.0" here — that triggers a C++ source compile
# which requires cmake/boost/lapack and takes 5-10 min, timing out on Railway.
# dlib-bin==19.24.6 and face_recognition are also installed explicitly in nixpacks.toml
# and Dockerfile BEFORE this file is processed, so they are already present.
dlib-bin==19.24.6
face_recognition>=1.3.0

# cache-bust 2026-04-13 13:47:38
